// Декларативний спосіб
function multiply(a, b, c, d) {
    return a * b * c * d;
}

// Анонімна функція
const multiplyAnon = function(a, b, c, d) {
    return a * b * c * d;
};

// Стрілочна функція
const multiplyArrow = (a, b, c, d) => a * b * c * d;